# Reduce GNOME RAM usage
Simple script for reduce memory consumption in GNOME 3. Tested on Fedora 42 Worksation with GNOME 48.

### Install and run:

```Shell
curl -fsSL https://raw.githubusercontent.com/tim77/reduce-gnome3-ram-usage/master/reduce-gnome3-ram-usage.sh | sh
```

Reboot.
